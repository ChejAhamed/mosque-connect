# NextAuth Configuration
# This URL is crucial for authentication callbacks
NEXTAUTH_URL=http://localhost:3000
# Secret used to encrypt cookies and tokens (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-secure-random-string-here

# Database Configuration
DATABASE_URI=mongodb+srv://username:password@cluster0.mongodb.net/mosque-connect?retryWrites=true&w=majority

# API Keys
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Only needed if using email provider for auth
# EMAIL_SERVER=smtp://username:password@smtp.example.com:587
# EMAIL_FROM=noreply@example.com

# Optional environment variables
# NODE_ENV=development # Set automatically by Next.js in dev/prod
# VERCEL_URL # Set automatically by Vercel in production

# Deployment mode (optional)
# APP_ENV=development # Can be development, staging, or production
